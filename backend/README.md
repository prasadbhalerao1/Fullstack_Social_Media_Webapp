# SocialMedia Backend

Minimal Express + Mongoose boilerplate.

Setup

1. Copy `.env.example` to `.env` and update values.
2. Install dependencies:

```bash
npm install
```

Run

```bash
npm run dev
```

API

- `GET /` - health check
- `POST /api/auth/register` - register
- `POST /api/auth/login` - login
